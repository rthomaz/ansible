- name: instalando pacotes extras para o ansible
  apt:
    name: "{{ packages }}"
  vars:
    packages:
    - python-pexpect
- name: create a new user
  user: name=rodrigo
    state=present
    groups="sudo"
    password=b3b3xu!@#
    comment=""
