- name: instalar ntp update
  apt:
    name: "{{ packages }}"
  vars:
    packages:
    - ntpdate
    - ntp-doc
- name: atualizar relógio com servidor {{ntp_update_server}}
  command: ntpdate {{ntp_update_server}}